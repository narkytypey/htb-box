import * as React from 'react';
import { Fineprint } from '@donerup/ui';

export const DirectoryNotice = () => (
  <Fineprint>
    Authenticating against the corporate LDAP directory.
    <br />
    Contact IT Service Desk for access requests.
  </Fineprint>
);
